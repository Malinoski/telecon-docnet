'use strict';

describe('myApp.addresses module', function() {

	beforeEach(module('myApp.addresses'));

	describe('addresses controller', function(){

		it('should ....', inject(function($controller) {
			
			var addressesCtrl = $controller('AddressesCtrl');			
			expect(addressesCtrl).toBeDefined();
			
		}));
		
 	});
	
});