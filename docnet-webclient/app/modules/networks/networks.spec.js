'use strict';

describe('myApp.networks module', function() {

	beforeEach(module('myApp.networks'));

	describe('networks controller', function(){

		it('should ....', inject(function($controller) {
			
			var networksCtrl = $controller('NetworksCtrl');			
			expect(networksCtrl).toBeDefined();
			
		}));
		
 	});
	
});